"use client"

import withAuth from "@/utils/withAuth";

const RecipeFeed = () => {
    return (
        <div>
            RecipeFeed Component
        </div>
    );
};

export default withAuth(RecipeFeed);